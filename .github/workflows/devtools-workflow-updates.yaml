on:
  workflow_dispatch:

name: foo

jobs:
  success-check:
    runs-on: ubuntu-latest
    steps:
      - uses: immich-app/devtools/actions/success-check@68f10eb389bb02a3cf9d1156111964c549eb421b # success-check-action-0.0.4
        with:
          needs: ""
